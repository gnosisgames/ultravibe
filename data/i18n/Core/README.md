# Core localization

Core strings live in the **Gnosis Engine** package:

`com.gnosisgames.gnosisengine/Runtime/Resources/i18n/Core/`

Do not add `{lang}.json` files here — Unity would merge duplicates and last-writer-wins would be unpredictable.
